# Fibrito
Sistema backend-first orientado a microservicios que implementa un algoritmo de reajuste calórico dinámico basado en series temporales de peso e ingesta, con persistencia en MongoDB y despliegue containerizado mediante Docker.
